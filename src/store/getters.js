import state from "./state";

export const currentChapter = state => state.currentChapter;

export const maxChapter = state => state.maxChapter;

export const showNav = state => state.showNav;

export const showSelectFont = state => state.showSelectFont;

export const modeNight = state => state.modeNight;

export const currentColor = state => state.currentColor;

export const fontSize = state => state.fontSize;